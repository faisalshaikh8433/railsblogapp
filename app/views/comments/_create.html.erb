<h2>Add Comment</h2>


<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
	<%= form.lable :username %><br>
	<%= form.text_field :username %>
	<%= form.lable :body %><br>
	<%= form.text_field :body %>
	<%= form.submit({:class => 'btn btn-danger'}) %>
<% end %>	